"""
TODO
        你正在爬樓梯。需要走 n 幾步才能到達頂部。
        每次你可以選擇爬山 1 或 2 走樓梯。有多少種不同的方式可以爬到最上面？
TODO
        範例 1：
        輸入： n = 2
        輸出： 2
        解釋：有兩種方法可以爬到頂部。
        1. 1 步 + 1 步
        2. 2 步
TODO
        範例 2：
        輸入： n = 3
        輸出： 3
        解釋：有三種方法可以爬到頂部。
        1. 1 步 + 1 步 + 1 步
        2. 1 步 + 2 步
        3. 2 步 + 1 步

TODO
        動態規劃 (Dynamic Programming)：
        由於問題存在最優子結構和重疊子問題，動態規劃是解決此類問題的理想選擇。我們將大問題分解為更小的子問題 D(n−1) 和 D(n−2)。
        迭代優於遞歸：
        時間效率：遞歸（未經優化）的時間複雜度為 O(2n)，而迭代或記憶化搜索的時間複雜度是線性的 O(n)。
        空間效率：
        您的斐波那契數解法（記憶化搜索）需要一個大小為 n 的字典來儲存中間結果，空間複雜度為 O(n)，且遞歸調用棧也需要 O(n) 的額外空間。
        迭代動態規劃（如上方代碼所示）僅使用兩個變量 (a 和 b) 來儲存必需的前兩步結果。這將空間複雜度優化至**O(1)** (常數級)，這是最優的空間效率。
        綜上，迭代動態規劃 以 O(n) 的時間複雜度、O(1) 的空間複雜度解決了問題，是此題的最優解。
"""
class Solution:
    def climbStairs(self, n: int) -> int:
        # 1. 處理邊界條件
        if n == 1:
            return 1

        # 2. 初始化：使用 O(1) 空間來儲存前兩步的結果

        # 'a' 儲存 D(i-2) 的結果
        # D(1) = 1
        a = 1

        # 'b' 儲存 D(i-1) 的結果
        # D(2) = 2 (1+1 或 2)
        b = 2

        # 3. 從第 3 級樓梯開始迭代計算
        # 我們已經知道 D(1) 和 D(2)，所以只需要計算 (n - 2) 次
        for _ in range(3, n + 1):
            # temp 儲存當前 D(i) = D(i-1) + D(i-2)
            temp = a + b

            # 更新 'a' 和 'b'，為下一步計算做準備：
            # a (D(i-2)) 變成 D(i-1)
            a = b
            # b (D(i-1)) 變成 D(i)
            b = temp

        # 4. 循環結束後，b 儲存的就是 D(n) 的結果
        return b


if __name__ == '__main__':
    s = Solution()
    q = 2
    print(f'q: {q}, ans: {s.climbStairs(q)}')

    q = 3
    print(f'q: {q}, ans: {s.climbStairs(q)}')